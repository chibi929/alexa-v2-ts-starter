{
  "name": "sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "ask-sdk-core": "^2.0.5",
    "ask-sdk-model": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^10.1.2",
    "typescript": "^2.8.3"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -f *.js *.js.map && rm -f src/*.js src/*.js.map",
    "predeploy": "npm run build",
    "deploy": "cd ../../ && apex deploy idiom",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
